<section class="sp">
  <div class="container sp__inner">
    <div class="sp__left">
      <h2 class="sp__title">We know what students need,<br/>1 M+ papers prove it</h2>
      <p class="sp__sub">We've earned their trust and media recognition too</p>
    </div>
    <div class="sp__right">
      <div class="sp__stars">★★★★★</div>
      <blockquote class="sp__quote">
        "ThesisArcPro helped me complete my PhD dissertation when I was completely overwhelmed. The expert assigned to me was incredibly knowledgeable and professional. I passed my defence with distinction!"
      </blockquote>
      <div class="sp__author">
        <div class="sp__avatar">SM</div>
        <div class="sp__author-info">
          <strong>Sarah M.</strong>
          <span>PhD Student, University of London</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
.sp {
  background: var(--color-secondary-lightest, #eef6fb);
  padding: 3.5rem 0;
}
.sp__inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
}

/* ── Left side ── */
.sp__title {
  font-family: var(--font-heading, 'Poppins', sans-serif);
  font-size: clamp(1.3rem, 3vw, 1.7rem);
  font-weight: 700;
  color: var(--color-main, #303b4f);
  line-height: 1.3;
  margin-bottom: 0.6rem;
}
.sp__sub {
  color: var(--color-main-light, #6d7483);
  font-size: 0.9rem;
}

/* ── Right side (testimonial) ── */
.sp__stars {
  color: #ffd20a;
  font-size: 1.2rem;
  margin-bottom: 0.8rem;
  letter-spacing: 2px;
}
.sp__quote {
  font-size: 0.92rem;
  color: var(--color-main, #303b4f);
  line-height: 1.7;
  font-style: italic;
  margin-bottom: 1.2rem;
  border: none;
  padding: 0;
}
.sp__author {
  display: flex;
  align-items: center;
  gap: 0.7rem;
}
.sp__avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--color-secondary, #046fcd);
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
  font-weight: 700;
}
.sp__author-info {
  display: flex;
  flex-direction: column;
}
.sp__author-info strong {
  font-size: 0.88rem;
  color: var(--color-main, #303b4f);
}
.sp__author-info span {
  font-size: 0.78rem;
  color: var(--color-secondary, #046fcd);
}

@media (max-width: 700px) {
  .sp__inner { grid-template-columns: 1fr; gap: 2rem; }
}
</style>